syntax = "proto3";


service AuthService {
    rpc TokenRequest (TokenRequest) returns (TokenResponse) {};
}

message TokenRequest {
    string token = 1;
}

message TokenResponse {
    bool status = 1;
}
